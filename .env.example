# hence-incognito — every value empty by default.
# Convention borrowed from the main app: an EMPTY value means the feature is OFF and
# the code path no-ops. Never ship a default that silently enables something.

# ── Identity ────────────────────────────────────────────────────────────────
# SAME Privy app as the main Hence app — that is what makes it the same user.
# incognito.hence.markets must be added to Privy's allowed origins or login fails.
VITE_PRIVY_APP_ID=

# Which phase's claims the UI is allowed to make. 1 = shielded execution (identity hidden,
# position public). 2 = bounded netting (matched orders never reach a public venue).
# DEFAULTS TO 1 IF UNSET OR WRONG — a misconfigured deploy must underclaim, never overclaim.
# Only set this to 2 once crossing is actually running.
VITE_PHASE=1

# ── Inco Lightning ──────────────────────────────────────────────────────────
# 'testnet' = Base Sepolia (default, use this first) · 'mainnet' = Base
VITE_NETWORK=testnet
# Deployed to Base Sepolia 2026-08-15, block 45478687
#   0xB20D08b74710294984D83c7ae109Cc691F3a42fc
# Left EMPTY here on purpose: .env.example is the template, and the ticket must refuse to
# place when it is unset. Put the address in web/.env.local.
VITE_INCOGNITO_CONTRACT=

# ── Avantis (Base) ──────────────────────────────────────────────────────────
# VERIFIED 2026-08-15 against the live GET /v2/meta — no longer second-hand research.
# Addresses live in web/src/lib/avantis.ts; only the endpoints and the builder code are env.
#
# The published SDK is PYTHON ONLY (avantis_trader_sdk) and we do not need it: it wraps a
# plain HTTP API. Build -> sign -> submit, from TypeScript, no SDK.
AVANTIS_TX_BUILDER=https://tx-builder.avantisfi.com
AVANTIS_TX_BUILDER_TESTNET=https://tx-builder-testnet.avantisfi.com
# Builder/partner attribution. Avantis DOES have builder codes (referral contract
# 0x1A110bBA13A1f16cCa4b79758BD39290f29De82D + a 'referral' EIP-712 domain), which answers
# the open monetisation question. Register one and set it here; empty = unattributed.
AVANTIS_BUILDER_CODE=

# ── Omnibus funder (keeper only — NEVER a VITE_ var) ────────────────────────
# Funds each newly created shielded wallet so it can pay Inco's fee (1e12 wei) + gas.
#
# THIS KEY MUST NEVER REACH THE BROWSER. Anything prefixed VITE_ is compiled into the public
# bundle; this deliberately is not. The web app can only ASK the keeper to fund an address.
#
# It is also the ANONYMITY SET: funding a shielded wallet from the user's OWN wallet would let
# anyone link the two in seconds. One shared funder is what breaks that link, so the crowd you
# hide in is everyone else it has paid.
#
# Fund it with the test budget only — the caps below bound a bug, not a compromise.
OMNIBUS_KEY=
FUND_GRANT_ETH=0.002          # per wallet — a few orders' worth, not a balance worth stealing
FUND_TOPUP_BELOW_ETH=0.0005   # do not top up above this, or repeat calls drain the funder
FUND_TOTAL_BUDGET_ETH=0.05    # hard ceiling across every wallet, ever

# ── Keeper ──────────────────────────────────────────────────────────────────
# Omnibus funder. THIS IS THE ANONYMITY SET — fund it with the test budget only,
# never a treasury key. Keep the balance capped.
OMNIBUS_PRIVATE_KEY=
# 120s: long enough that a seeded book cannot straddle an epoch close (a split book can leave
# both halves under MIN_ORDERS_TO_REVEAL, permanently), short enough that a live order nets
# while the room is still watching.
EPOCH_SECONDS=120
DATABASE_URL=

# ── Access ──────────────────────────────────────────────────────────────────
# Comma/space-separated wallet addresses allowed into the prototype. SERVER-SIDE ONLY —
# deliberately NOT prefixed VITE_, because anything the Vite client can read is compiled
# into the public bundle. The browser only ever learns a yes/no about its own address.
# EMPTY = nobody gets in (fail closed). This is the opposite of the main app's rebate
# whitelist, where empty means "open to everyone" — do not copy that convention here.
TEAM_WALLETS=

# ── Safety rails ────────────────────────────────────────────────────────────
MAX_ORDER_USD=100
DRY_RUN=1

# ---- seeding a book (keeper: npm run seed) ----------------------------------------
# Netting is per market, and so is the reveal floor — every seed order must go into ONE market
# or you get three thin books that each refuse to publish.
SEED_PAIR=ETH                 # ETH | BTC | SOL  (Avantis pair indices 0 | 1 | 2)
SEED_LONGS=12000,9500,8000,6200,4400
SEED_SHORTS=11000,7300,5500,3700
SEED_GAS_ETH=0.0015           # per throwaway wallet; keys are discarded after use

# ---- background flow (keeper: keeps epochs from being empty) ------------------------
# OFF by default and REFUSES on mainnet. Each order is a real transaction from a real wallet,
# encrypted and netted like any other — what is synthetic is only that one operator controls
# them all. /api/health reports filler.active so that stays inspectable. Disclose it.
FILLER_ENABLED=0              # '1' to arm
FILLER_ORDERS=4               # per round, one round per epoch
FILLER_PAIRS=1                # Avantis pair indices; ONE market — thin flow spread thin crosses nothing
FILLER_GAS_ETH=0.0008         # per throwaway wallet
FILLER_BUDGET_ETH=0.01        # hard ceiling on everything it will ever spend
